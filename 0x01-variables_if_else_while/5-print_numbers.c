#include <stdio.h>

/**
 * main - Entry point
 * Description: Display numbers from 0 to 9
 * Return: return 0 and exit
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);

	printf("\n");
	return (0);
}
