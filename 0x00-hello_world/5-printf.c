#include <stdio.h>

/**
 * main - Entry point
 * Description: Print text with printf()
 * Return: 0 and exit
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
